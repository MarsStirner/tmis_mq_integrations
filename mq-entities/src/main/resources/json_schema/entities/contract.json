{
  "id": "http://bars-open.ru/medvtr/json-schema/contract.json",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Contract",
  "description": "Договор на лечение (контракт)",
  "type": "object",
  "javaType": "ru.bars_open.medvtr.mq.entities.Contract",
  "properties": {
    "id": {
      "description": "Идентифкатор",
      "type": "integer"
    },
    "payer": {
      "description": "плательщик",
      "$ref": "./person.json"
    },
    "number": {
      "description": "номер договора",
      "type": "string"
    },
    "signDate": {
      "description": "дата+врем подписания договора",
      "type": "string",
      "format": "date-time"
    },
    "begDate": {
      "description": "дата+время начала действия договора",
      "type": "string",
      "format": "date-time"
    },
    "endDate": {
      "description": "дата+время окончания действия договора",
      "type": "string",
      "format": "date-time"
    },
    "finance": {
      "type": "object",
      "$ref": "./refbook/finance.json",
      "description": "Источник финансирования"
    }
  },
  "required": [
    "id",
    "externalId"
  ]
}

}